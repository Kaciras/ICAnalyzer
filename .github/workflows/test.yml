name: Test

on: push

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Use Node.js
        uses: actions/setup-node@v3
        with:
          node-version: latest

      - name: Checkout code
        uses: actions/checkout@v3
        with:
          submodules: true

      - run: npm i -g pnpm

      - run: pnpm i

      - run: pnpm run build

      - run: pnpm run test
